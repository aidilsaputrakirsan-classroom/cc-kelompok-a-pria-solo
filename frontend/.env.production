# Production API & app URLs (Modul 11 — boleh di-commit, tidak berisi secret).
# Salin nilai ini ke Railway → frontend service → Variables.

APP_NAME="PRIA Solo"
APP_ENV=production
APP_DEBUG=false
APP_URL=https://cc-kelompok-a-pria-solo-production.up.railway.app

# FastAPI backend (tanpa slash di akhir)
URL_VM_PYTHON=https://backend-production-bdd8.up.railway.app

# DB_* di Railway diisi dari service MySQL (lihat docs/deployment-guide.md)
