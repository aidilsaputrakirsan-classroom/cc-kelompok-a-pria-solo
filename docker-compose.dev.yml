# Development overrides — hot reload (Modul 12 bonus)
# Usage: docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build

services:
  document-service:
    volumes:
      - ./backend:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8001 --reload
